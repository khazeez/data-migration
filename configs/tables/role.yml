sheet:
  spreadsheet_id: YOUR_SPREADSHEET_ID
  worksheet: Role

table: m_role

mapping:
  Role Name:
    column: name
    required: true
  Description:
    column: description

defaults:
  id: uuid
  active: true
  created_at: now
  updated_at: now
  created_by: system
