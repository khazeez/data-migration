sheet:
  spreadsheet_id: 1xxxxxxxx
  worksheet: Role

table: m_role

mapping:
  Role Name:
    column: name

defaults:
  id: uuid
  active: true
  created_at: now
  updated_at: now
  created_by: system

# Opsional: atur perilaku saat ada duplicate key
# on_conflict:
#   action: identify   # update hanya jika ada perubahan nilai
#   keys: [name]       # kolom constraint (harus unique index di DB)